I can see the `.env` file contains two API keys. What updates would you like me to make to the `.env` file? For example:

- **Add** new environment variables (e.g. a new API key, model name, base URL)?
- **Update** existing values (e.g. replace one of the API keys)?
- **Remove** a variable?
- **Add** a `.env.example` template alongside it?

Please share what you'd like changed and I'll apply it right away.